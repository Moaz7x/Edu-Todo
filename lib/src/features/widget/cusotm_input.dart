import 'package:flutter/material.dart';

class ZInput extends StatelessWidget {
  const ZInput({super.key});

  @override
  Widget build(BuildContext context) {
    return TextField(
      
    );
  }
}